{
  "builds": [
    {
      "src": "dist/main.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js"
    }
  ],
  "installCommand": "pnpm install --no-frozen-lockfile && npx prisma generate",
  "buildCommand": "pnpm run build:prod",
  "env": {
    "NODE_ENV": "production"
  }
}
